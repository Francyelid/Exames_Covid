<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Configuração das bordas -->
    <stroke
        android:width="10dp"
        android:color="@color/default_red" />

    <!-- Configuração do interior do retângulo -->
    <solid android:color="@color/default_red" />

    <!-- Radio das bordas -->
    <corners
        android:radius="5dp"
        android:topRightRadius="100dp" />
</shape>